export const LEGAL_PROTOCOLS = ['https', 'http', 'file']

export const CONTAINER_ID = 'mind_toc_container'

// Do not show h1 in toc
export const SEMANTIC_HEADINGS = 'h2,h3,h4,h5,h6'.split(',')

// Reject these tags when extrating headings
export const IGNORED_TAGS = 'header,footer,nav,aside,ul,ol,table'.split(',')

export const TEXT_DENSITZY_THRESHOLD = 10

export const LINK_DENSITY_THRESHOLD = 0.3
